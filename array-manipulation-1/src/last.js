/* exported last */

/*
    1. declare a function name last with 1 parameter array
    2. get the last element of array with length - 1 and use this expression as the index
*/

function last(array) {
  return array[array.length - 1];
}
