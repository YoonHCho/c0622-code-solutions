/* exported head */

/*
    1. declare a function with name head with 1 parameter array
    2. get the first element of array with index 0, arrays are zero indexed: the first element will always have a 0 index, first element and return
*/

function head(array) {
  return array[0];
}
