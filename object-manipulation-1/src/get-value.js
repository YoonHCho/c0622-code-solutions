/* exported getValue */

/*
    - declare function named getValue with 2 parameters object and key(string)
    - object at key will return out of function
*/

function getValue(object, key) {
  return object[key];
}
