/* exported takeValueAtIndex */

function takeValueAtIndex(queue, index) {
  // let counter = 1;
  // if (queue.peek() === undefined) return;
  // let first = queue.dequeue();
  // while (queue.peek() !== undefined || counter <= index) {
  //   if (counter === index) {
  //     queue.dequeue();
  //     return queue;
  //   }
  //   queue.enqueue(first);
  //   first = queue.dequeue();
  //   counter++;
  // }
  // return queue;

  // while (counter <= index) {
  //   if (counter === index) {
  //     queue.dequeue();
  //     return queue;
  //   } else if (index === 0) return 0;
  //   const toBack = queue.dequeue();
  //   if (queue.peek() === undefined) {
  //     counter++;
  //     return '<empty>';
  //   }
  //   counter++;
  //   console.log(toBack);
  //   queue.enqueue(toBack);
  // }
  // return queue;
}
