/* exported chunk */

//     declare function named chunk with 2 parameter (array, size)
//     initialize variable start and end, start = 0, end = size,

// function chunk(array, size) {
//   var endIndex = size;
//   for (var i = 0; i < array.length / size)

// }
