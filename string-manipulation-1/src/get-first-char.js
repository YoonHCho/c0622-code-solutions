/* exported getFirstChar */

/*  Pseudo code
    1. write a function declared with name getFirstChar with one parameter string
    2. need to look at the string and locate the first character, index at 0
    3. return that string at index# from the function
*/

function getFirstChar(string) {
  return string[0];
}
